console.log("hi");

//when onload:
// - banner section and serach form will always there

//search history
// - if has in LS then render the city name one by one for the top 8 most resent search
// - if not render massage : no previous search history.
// - add clear all searches button
